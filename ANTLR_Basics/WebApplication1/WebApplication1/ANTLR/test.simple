fehlerTest = new List();
fehlerTest.add("Nur ein Element");

// Dies sollte (je nach deiner Java-Logik) eine Warnung oder Fehler werfen
// print(fehlerTest.getByIndex(10)); 

// Prüfe Überschreiben im Dictionary
alter = new Dictionary();
alter.put("Max", 20);
alter.put("Max", 25); // Überschreibt 20
print("Max Alter (sollte 25 sein):");
print(alter.getBy("Max"));